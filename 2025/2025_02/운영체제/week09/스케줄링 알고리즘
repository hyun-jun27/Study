스케줄링 알고리즘 : 문제를 해결하는 과정

  선입선처리 스케줄링 : 그냥 큐랑 똑같은 선입선출 / 반환시간, 대기시간 발생 시간 계산 가능
    
    호위효과 : 프로세스가 작동중인 다른 프로세스 떠나가기를 기다리는 것

  최소작업 우선 스케줄링 : 말 그대로 여러 작업 중 더 작은 시간인 것을 앞으로 나와 "최소작업 먼저"
    장점 : 가장 평균 대기 시간이 짧음
    단점 : 불공정한 대기 시간 발생가능함

  우선순위 스케줄링 : 말 그대로 우선순위가 중요한 것부터 사용
    근데 우선 순위가 밀린 것은 계속 기다려야 하기에 에이징(밀릴 수록 우선순위가 올라가짐)을 씀

  라운드 로빈 스케줄링 : 시분할 개념으로 동시에 처리함

  다단계 큐 스케줄링 : 각 작업을 서로 다른 묶음으로 분류
  
