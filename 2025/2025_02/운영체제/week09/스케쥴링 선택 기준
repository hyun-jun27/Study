스케쥴링 : 프로세스에게 자원할당 하는 것

  프로세스 실행 과정 : 프로세서 버스트 -> 입출력 버스트 -> 프로세서 버스트 / 반복함 프로그램 실행하고 데이터 받을 때까지 기다리고 그 데이터로 실행하고 계속 반복함

  스케쥴링 수행 단계
    1단계 : 작업 선택 /시스템 자원을 사용할 프로세스를 정하는 것 / 승인 스케줄링이라고도함
    2단계 : 사용 권한 부여 / 사용 권한 부여할 프로세스 결정하는 작업 승인과 프로세서 할당 스케줄링
    3단계 : 준비 상태의 프로세스에 프로세서 할당(디스패칭)

  큐잉 도표


  선점자원 : 운영체제가 준 메모리 뺏을 수 있음
  비선점자원 : 못 뺏음

  중요한 평가요소 : / 처리율 / 반환시간 / 대기시간 / 반응시간
  대기시간 : 프로세스가 준비큐에 있는 시간
  응답시간 : 첫 출력까지 걸린 시간
  실행시간 :  실행시작부터 종료까지의 시간
  반황시간 : 종료까지 시간
    
